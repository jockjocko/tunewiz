import { Container, Grid } from '@mui/material';

const Home = () => {
    return (
        <Container>
            <Grid container spacing={2}>
                <Grid item xs={12}>
                    Home
                </Grid>
            </Grid>
        </Container>
    );
};

export default Home;
